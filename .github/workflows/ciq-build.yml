# .github/workflows/ciq-build.yml
name: CIQ Build

on:
  workflow_dispatch:
  push:
    paths:
      - "source/**"
      - "resources/**"
      - "manifest.xml"
      - "monkey.jungle"
      - "tools/**"

# Empêche les files d'attente interminables et annule les runs obsolètes
concurrency:
  group: ciq-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      # … le reste de tes étapes inchangé …
